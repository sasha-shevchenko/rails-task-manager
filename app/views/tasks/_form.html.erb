<%= form_for(task) do |f| %>
  Title:
  <%= f.text_field :title %>
  Details: <%= f.text_field :details %><br>
  Completed: <%= f.check_box :completed if Task.exists?(id: task.id) %><br>
  <%= f.submit %>
<% end %>
